filter:
   paths: ['app/*', 'resources/*', 'module.php']
   dependency_paths:
      - app/Maps/
   excluded_paths:
      - resources/mappings/
coding_style:
   php:
      indentation:
         general:
            size: 4
build:
   environment:
      php: 8.0